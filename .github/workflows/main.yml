# GitHub Actions Workflow: CI

name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x] # Test on multiple Node.js versions

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Lint
      run: npm run lint # Assuming you have a lint script in package.json

    - name: Type-check
      run: npx tsc --noEmit

    - name: Unit tests
      run: npm test # Assuming you have a test script in package.json

    # - name: Build artifact (example, adjust as needed)
    #   run: npm run build # Assuming you have a build script
    #   if: matrix.node-version == '20.x' # Only build on one version

    # - name: Archive production artifacts (example)
    #   if: matrix.node-version == '20.x'
    #   uses: actions/upload-artifact@v3
    #   with:
    #     name: build-artifact
    #     path: |
    #       dist/
    #       # Add other paths to archive if needed
